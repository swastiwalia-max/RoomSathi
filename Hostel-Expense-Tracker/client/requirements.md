## Packages
framer-motion | Smooth page transitions and layout animations
recharts | Data visualization for expense insights
date-fns | Date formatting for expense feeds

## Notes
- Tailwind Config needs to extend fontFamily for 'Poppins' (headings) and 'Inter' (body)
- localStorage will be used to persist session (userId, roomId) as there is no auth backend
- API base path is /api
- Mobile-first design approach
